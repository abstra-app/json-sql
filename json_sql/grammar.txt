select
    "select" fields from where order
fields
    field
    field "," fields
from
    "from" from_table
    "from" from_table join
from_table
    :name
    :name as :name
join
    "join" from_table on
    "left join" from_table on
    "right join" from_table on
    "inner join" from_table on
    "outer join" from_table on
on
    "on" expression
where
    "where" expression
order
    "order" "by" order_fields
order_fields
    order_field
    order_field "," order_fields
order_field
    expression
    expression "asc"
    expression "desc"
expression
    expression_value
    expression operator expression
    expression "(" expression ")"
